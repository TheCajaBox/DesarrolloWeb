// Vite resuelve las rutas absolutas del HTML contra `root`, que aqui es esta
// misma carpeta. Por eso el HTML carga este fichero y es este el que importa
// el codigo compartido, ya por alias.
import '@/taller.js'
